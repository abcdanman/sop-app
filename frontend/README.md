Frontend README

1) Install dependencies: npm install
2) Start dev server: npm run dev

NOTE: Vite dev server proxies /api to your backend. For development, run backend on port 5000 and configure proxy in `vite.config.js` or use simple CORS on backend.
